#ifndef CHROME_GREEN_SRC_HOTKEY_H_
#define CHROME_GREEN_SRC_HOTKEY_H_

// Registers the boss-key hotkey (if configured). Safe to call once at startup.
void GetHotkey();

#endif  // CHROME_GREEN_SRC_HOTKEY_H_
